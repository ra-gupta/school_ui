<div class="mb-6 flex flex-wrap items-center justify-between gap-3">
  <h1 class="text-2xl font-semibold"><%= title %></h1>
  <div class="flex gap-2"><%= yield_actions if defined?(yield_actions) %><%= actions if defined?(actions) %></div>
</div>
