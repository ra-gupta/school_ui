<% flash.each do |type, message| %>
  <div class="mb-4 rounded-lg border px-4 py-3 text-sm <%= type.to_s == "alert" ? "border-red-200 bg-red-50 text-red-800" : "border-emerald-200 bg-emerald-50 text-emerald-800" %>">
    <%= message %>
  </div>
<% end %>
